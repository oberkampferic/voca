void master::question() {
  i_num = random() % i_limit;
  str_num = to_string(i_num);
  s_question = str_num;
}

//----------------------------------------------------------------------------------------------------

int master::reponse() {
  i_reponse= stoi(s_reponse);
}

//----------------------------------------------------------------------------------------------------

void master::question2() {
  int i_minutes, i_heures;
  
  i_num = random() % (60*12);

  i_heures = i_num % 60;
  i_minutes = i_num - (i_heures * 60);


  s_question = "Es ist ";
  
  if (i_minutes==0) {
    s_question = s_question + to_string(i_heures) + " Uhr."
  } 
  if (minutes==30) {
    s_question = s_question + " halb " + to_string( i_heures + 1 ) + " Uhr."
  }
  if (minutes==10){
    
  }
  if (minutes==50){
    
  }
  if (minutes<30) {
    
  }
  if (minutes>30) {
    
  }
}

//----------------------------------------------------------------------------------------------------

int master::reponse2() {
  i_reponse= stoi(s_reponse);
}
