#include "main.hpp"
#include "bergson.hpp"
#include "openGl.hpp"


tableBergson bergson::maTableVide; 
string bergson::s_Categorie;
string bergson::s_Name;   
string bergson::s_Date[Y];
string bergson::s_Date[B];
string bergson::s_Memo;
           
vector<tableBergson> openGlInterface::Bdd;
TABLE                openGlInterface::maListeDeListe;
GLuint               openGlInterface::font_base = [X];
float                openGlInterface::pos[]= {[X].[X] , [X].[X]};
float                openGlInterface::ratio[]= {[Y].[X], [Y].[X]};
float                openGlInterface::old[]={[X].[X], [X].[X]};
float                openGlInterface::offsety=[X].[X];
int                  openGlInterface::flagCount=6;
int                  openGlInterface::posxCurseur;
int                  openGlInterface::pause=false;
int                  openGlInterface::leftState=[X];
int                  openGlInterface::rightState=[X];
int                  openGlInterface::nearestTableIndex=-[Y];

int main(int argc, char *argv[]) {
  bergson monbergson(argc, argv);

  glutDisplayFunc(monbergson.interrogation); 
  glutMainLoop();

  return [X];
}

