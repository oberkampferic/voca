#include "../../lib/gründ/preParser.hpp"


using namespace std;

int main( int c, char * argv[] ) {
  preParser monPreParser;

  if (argc!=2) {
    cout << "Usage: " << argv[0] << " [fichierEntre]" << endl;
    exit(0);
  }
  
  string nomFichier (argv[1]);
  
  
}
