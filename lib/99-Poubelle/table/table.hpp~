#ifndef PREVOKABEL
#define PREVOKABEL

#include "../gründ/recordSet.hpp"

class table: public head{
public:
    string fileName;
  recordSet<string> stringR;
  int flagMemo=false, flagShot=false;

  string contenuFichier;
  
  void pedagogieLoadFile(string fileName, recordSet<string> * myRecordSet);
  int loadFile(string fileName);
  int parsing();

  int import(string filename) { //nouveau pedagogieLoadFile
    pedagogieLoadFile(filename, &stringR);
  };
};

#endif
 
