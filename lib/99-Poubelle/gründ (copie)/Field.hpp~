#ifndef FIELD_HPP
#define FIELD_HPP

//#include "../../main.hpp"
#include "commun.hpp"

class Field: virtual public commun {
public:
  string col;
  int level;
  long time;

  Field();
  int store(string test,
	    size_t *pos1, size_t *pos2,
	    int flagSuperStatic);
  int reset();
  int print();
  int saveHumain(ostream * out);
  int saveShort(ostream * out);
  int fautIlRedemander();
  int reussite();
  int echec();
};

#endif
